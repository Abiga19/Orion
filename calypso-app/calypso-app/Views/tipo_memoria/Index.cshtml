@model IEnumerable<calypso_app.Models.tipo_memoria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_tipomemoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus_tipomemoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion_tipomemoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus_tipomemoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipomemoria }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipomemoria }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipomemoria })
        </td>
    </tr>
}

</table>
